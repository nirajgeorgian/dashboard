import PlacesAutocomplete from './AutoComplete';
import { geocodeByAddress, geocodeByPlaceId, getLatLng, getDetails } from './utils';

export { geocodeByAddress, geocodeByPlaceId, getLatLng, getDetails };

export default PlacesAutocomplete;
